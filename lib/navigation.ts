/**
 * Transition type tags for Next.js Link / router.push (experimental.viewTransition).
 * Matched in CSS via :active-view-transition-type(...).
 */
export const NAV_TRANSITION = ['nav'] as const;
export const NAV_FORWARD = ['nav-forward'] as const;
export const NAV_BACK = ['nav-back'] as const;
